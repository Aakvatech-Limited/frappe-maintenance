{
  "organization": "Aakvatech-Limited",
  "repository_include_regex": ".*",
  "repository_exclude_regex": "^frappe-maintenance$",
  "default_branch_only": true,
  "files": [
    {
      "source": "templates/label-failed-prs.yml",
      "target": ".github/workflows/label-failed-prs.yml",
      "mode": "create_or_update"
    }
  ],
  "work_branch_prefix": "automation/label-failed-prs",
  "commit_message": "chore: add failed PR action labeling",
  "pr_title": "chore: add failed PR action labeling",
  "pr_body": "Adds the organization-managed workflow that labels open pull requests with `action-failed` when pull-request GitHub Actions fail, and removes the label after the current PR head has no failed runs. The workflow can also be run manually to reconcile all open PRs."
}
